mapscripts Wormhole_Entrance_MapScripts {
    MAP_SCRIPT_ON_TRANSITION {
        if(flag(FLAG_ORTHWORM_POTION)) {
            setmaplayoutindex(LAYOUT_WORMHOLE_ENTRANCE_LAYOUT_2)
        }
        setrespawn(HEAL_LOCATION_WORMHOLE_ENTRANCE)
    }
}
